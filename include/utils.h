#ifndef  _UTILS
#define  _UTILS

#include "spatial_coords.h"

 int spatial_coords_2_index(spatial_coords);
 spatial_coords index_to_xy(int);
 double min_value(double, double);
 double max_value(double, double);

#endif